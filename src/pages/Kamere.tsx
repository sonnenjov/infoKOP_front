import "../styles/main.css"
import { Season } from "../hooks/useSeason"
import WeatherReport from "../components/WeatherReport";

interface Props {
  activeSeason: Season;
}

export default function Kamere({ activeSeason }: Props) {
  return (
    <main >
      
    </main>
  )
}